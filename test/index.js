var expect = require('expect.js'),
    sinon = require('sinon'),
    path = require('path');

describe('Test', function() {
    beforeEach(function() {
        //some before each here
    });
   describe('#test()', function() {
       it('should work', function() {
           expect(true).to.be(true);
       });
   });
});